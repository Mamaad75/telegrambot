import crypto from "node:crypto";
export function validateTelegramInitData(initData,botToken,maxAgeSeconds=86400){const p=new URLSearchParams(String(initData||"")),hash=p.get("hash");if(!hash)throw new Error("Missing Telegram hash");const age=Math.floor(Date.now()/1000)-Number(p.get("auth_date")||0);if(age<0||age>maxAgeSeconds)throw new Error("Telegram initData expired");p.delete("hash");const check=[...p.entries()].sort(([a],[b])=>a.localeCompare(b)).map(([k,v])=>`${k}=${v}`).join("\n");const secret=crypto.createHmac("sha256","WebAppData").update(botToken).digest();const expected=crypto.createHmac("sha256",secret).update(check).digest("hex");if(expected.length!==hash.length||!crypto.timingSafeEqual(Buffer.from(expected),Buffer.from(hash)))throw new Error("Telegram initData signature invalid");return JSON.parse(p.get("user")||"{}")}
